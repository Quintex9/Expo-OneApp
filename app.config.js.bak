export default ({ config }) => ({
  ...config,
  plugins: [
    ...(config.plugins ?? []),
    './plugins/withMapboxMaven',
    './plugins/withAndroidXFix',
    './plugins/withGradleProperties',
    "expo-localization",
    "expo-asset",
  ],
  web: {
    ...config.web,
    bundler: 'metro',
    // Set viewport to iPhone size
    meta: {
      viewport: 'width=device-width, initial-scale=1.0, maximum-scale=1.0, user-scalable=no',
    },
  },
  extra: {
    ...config.extra,
    MAPBOX_TOKEN: "pk.eyJ1IjoianNnY2l0eSIsImEiOiJjbWJ6YjRmOWUwY2E1Mm9xeHJkbGY4a3V5In0.xH1_hbHZbdGtO6yamCy87w",
  },
});
